const Ammenda = () => {
    return (
        <p>se non hai ancora eseguito il push dei commit si può rimediare con un <code>git commit --amend</code> e modificare il messaggio su text editor</p>
    );
}

export default Ammenda;